---
description: 
globs: 
alwaysApply: false
---
1. 模块的接口都必须以虚接口的方式定义
```python

class ChatService(ABC):
    """聊天服务接口

    定义了聊天服务的基本功能，包括消息发送和上下文管理。
    """

    @abstractmethod
    async def get_response(self, message: str) -> str:
        """发送消息并获取回复

        Args:
            message: 要发送的消息

        Returns:
            str: 服务的回复

        Raises:
            ConnectionError: 连接错误
            ServiceError: 服务错误
        """
        pass
```
2. 一个子系统中唯一可以对外暴露的是接口，且经通过依赖注入容器暴露
3. 结构通过子系统的__init__.py对外暴露
   如：

